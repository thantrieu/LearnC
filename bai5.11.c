#include <stdio.h>
#include <string.h>

void hienThi(char s[100][20], int size);
void tachTu(char*);

void tachTu(char* s) {
	
}

void hienThi(char s[100][20], int size) {
	
}

int main(){
	char s[1000];
	fgets(s, 999, stdin);
	tachTu(s);
	return 0;
}

